<Window x:Class="Connect4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:Connect4.Converters"
        Title="Connect 4"
        Width="560"
        Height="680"
        MinWidth="480"
        MinHeight="600"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">

    <Window.Resources>
        <conv:PlayerToBrushConverter x:Key="PlayerToBrush"/>
        <Style x:Key="ColumnButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="8,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NewGameButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#283593"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3949AB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="CONNECT 4"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"/>

        <!-- Status bar -->
        <Border Grid.Row="1"
                Background="#283593"
                CornerRadius="10"
                Padding="16,12"
                Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Ellipse Width="20" Height="20"
                         Fill="{Binding CurrentPlayerBrush}"
                         Margin="0,0,12,0"
                         VerticalAlignment="Center"/>
                <TextBlock Text="{Binding StatusText}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="White"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Board area -->
        <Border Grid.Row="2"
                Background="{StaticResource BoardBlueBrush}"
                CornerRadius="16"
                Padding="12"
                BorderBrush="{StaticResource BoardDarkBlueBrush}"
                BorderThickness="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column drop buttons -->
                <UniformGrid Grid.Row="0" Rows="1" Columns="7" x:Name="ColumnButtonsPanel"/>

                <!-- Game board -->
                <Border Grid.Row="1"
                        Background="{StaticResource BoardDarkBlueBrush}"
                        CornerRadius="12"
                        Padding="8"
                        Margin="0,8,0,0">
                    <UniformGrid x:Name="BoardPanel" Rows="6" Columns="7"/>
                </Border>
            </Grid>
        </Border>

        <!-- New game button -->
        <Button Grid.Row="3"
                Content="New Game"
                Style="{StaticResource NewGameButtonStyle}"
                Command="{Binding NewGameCommand}"
                HorizontalAlignment="Center"
                Margin="0,20,0,0"/>
    </Grid>
</Window>
